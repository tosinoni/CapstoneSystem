{
  "name": "capstone-system",
  "description": "capstone system for final year students",
  "version": "0.0.1",
  "engines": {
    "npm": "5.6.x"
  },
  "scripts": {
    "heroku-postbuild": "npm install -g bower && bower install"
  }
}